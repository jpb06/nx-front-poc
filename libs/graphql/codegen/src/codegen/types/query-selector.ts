export type QuerySelector = {
  me?: {
    id?: boolean;
    email?: boolean;
    lastName?: boolean;
    firstName?: boolean;
    joinDate?: boolean;
    role?: boolean;
    token?: boolean;
  };
};
